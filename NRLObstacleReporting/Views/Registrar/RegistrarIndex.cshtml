@{
    ViewData["Title"] = "Registrar - Start";
}

<div class="flex flex-col items-center justify-start min-h-screen pt-10 pb-20 px-6 relative z-10">

    <!-- Kort / hovedcontainer -->
    <div class="bg-white/80 backdrop-blur-sm rounded-3xl shadow-lg p-10 sm:p-14 w-full max-w-xl mx-auto text-center space-y-10 -mt-6">

        <!-- Header -->
        <header class="space-y-2">
            <h1 class="text-4xl sm:text-5xl font-bold text-gray-900 leading-tight">
                Welcome, <span class="text-blue-500">Registrar!</span>
            </h1>
            <p class="text-lg sm:text-xl text-gray-600">
                Review and manage obstacle reports
            </p>
        </header>

        <!-- Knapp-seksjon -->
        <div class="grid gap-6 sm:gap-8">

            <!-- Primær knapp -->
            <a asp-action="RegistrarViewReports" asp-controller="Registrar"
               class="block w-full rounded-2xl py-6 sm:py-7 border-2 border-blue-600 
          bg-blue-500 text-white text-xl sm:text-2xl font-semibold 
          hover:bg-blue-600 hover:shadow-lg hover:scale-[1.02] active:scale-[0.98] 
          transition-all duration-200 ease-out">
                View Reports
            </a>
            
        </div>
    </div>
</div>