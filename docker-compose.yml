services:
  db:
    image: mariadb:11
    restart: unless-stopped
    container_name: db
    environment:
      MYSQL_ROOT_PASSWORD: superpass
      MYSQL_DATABASE: nrl
      MYSQL_USER: root
      MYSQL_PASSWORD: superpass
    volumes:
      - dbdata:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mariadb-admin ping -uroot -psuperpass --silent"]
      interval: 5s
      timeout: 5s
      retries: 20
    ports:
      - "8083:3306"
    networks:
      - appnet

  web:
    build:
      context: .
      dockerfile: NRLObstacleReporting/Dockerfile
    depends_on:
      db:
        condition: service_healthy
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      # Overstyrer appsettings.Development.json
      ConnectionStrings__DefaultConnection: server=127.0.0.1;port=8083;database=nrl;user=root;password=superpass;
    ports:
      - "8080:8080"
    restart: unless-stopped
    networks:
      - appnet

volumes:
  dbdata:

networks:
  appnet:
    name: appnet
    driver: bridge